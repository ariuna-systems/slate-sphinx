
MathJax Support (reStructuredText)
==================================

Inline math via the ``:math:`` role: :math:`\sin(x)`.

Inline math via dollar delimiters: $\sin(x)$.

Display math via the ``.. math::`` directive:

.. math::

   \int_{-\infty}^{\infty} e^{-x^2}\,dx = \sqrt{\pi}
